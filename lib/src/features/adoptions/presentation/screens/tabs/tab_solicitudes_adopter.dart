import 'package:flutter/material.dart';

class TabSolicitudesAdopter extends StatelessWidget {
  const TabSolicitudesAdopter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tab Solicitudes Adoptante'),
    );
  }
}
